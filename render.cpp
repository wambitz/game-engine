// render.cpp
#include "render.h"
#include <iostream>

namespace Engine {

void render() {
    std::cout << "Rendering from the dynamically linked library (librender.so)" << std::endl;
}

}  // namespace Engine